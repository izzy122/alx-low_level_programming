#include "main.h"

/**
 * _islower  - check the code.
 * @c - this is  the variable to be checked 
 * Return: 0 if it is lower and .
 */

int _islower(int c)
{

	if ( c >= 'a' && c <= 'z')
	return (1);

	else 
	return (0);
}
